---
name: japanese-longform-writing
description: |
  日本語の読者向け長文（note 記事・連載・教材・レポート・提案書・社外向け md）を書く、または既にある下書きを推敲・改稿するためのスキル。実測 130 件のレビュー指摘から抽出した癖カタログ 18 項目、文体を数値化する診断スクリプト、多観点レビューの担当設計を提供する。次のような指示で使用する: 「note の記事を書いて」「読者向けに書き直して」「この下書きを見直して」「文が固い／冷たい／淡白」「もっとナラティブに」「推敲して」「表現を校正して」「社内文書を社外向けに書き換えて」「多軸レビューして」。文体が平板・専門用語が多い・書き手の感情が見えない・話をまたいで矛盾している、といった問題の切り分けと修正に強い。Reverve Consulting の特定文体を模倣する場合は reverve-style-writer、Web ページの販売コピーは copywriting、文書作成のプロセス進行と要件引き出しは doc-coauthoring を使う（本スキルは日本語の文そのものの作法が対象）。英文ライティング・コードのコメント・Word の文字数カウント（docx-word-count）には使わない。
metadata:
  version: 1.0.0
  category: コミュニケーション
---

# 日本語の読者向け長文 — 執筆と推敲

## Critical: 3 つの層は、上から下が見えない

「文が固い」「読みにくい」という指摘は、**別々の 3 つの問題**を指している。
**上の層のレビューを何度回しても、下の層は絶対に出てこない。**

| 層 | 症状 | 検出方法 |
|---|---|---|
| **構成** | 何が言いたいか分からない | 内容レビューで出る → `references/structure.md` |
| **温度** | 書き手がどこにもいない・冷たい | 内容レビューでは出ない → `references/prose-tics.md` F1 |
| **手触り** | 一文一文が平板・淡白 | どちらでも出ない。**実測しないと見えない** → `scripts/prose_check.py` |

まず「どの層の話か」を切り分ける。切り分けずに手を動かすと、的外れな修正になる。

## Critical: 修正後に必ず再実測する

**手触りを直す作業は、癖を増やす方向に働く。**

実際の事故: 淡白さを直すために接続詞とダッシュと言い回しを足した結果、
同じ原稿で過去に潰した癖が復活した（`——` 0→18 個、ぼかし 3→11 個、「ところが」5→25 個）。

事前診断だけでは足りない。**修正後に `prose_check.py` を再実行して、
新しい癖を持ち込んでいないか確かめる。**

---

## 診断スクリプト

```bash
python scripts/prose_check.py "draft/*.md"

# 内輪語を指定して検出（そのプロジェクト固有の語）
python scripts/prose_check.py "draft/*.md" --jargon 玉,刺さる,乖離,閉じる

# 免責文など毎回同じ定型を除外
python scripts/prose_check.py "draft/*.md" --exclude "本記事は|免責"

# 該当行を全件表示 / JSON 出力
python scripts/prose_check.py "draft/*.md" --detail
python scripts/prose_check.py "draft/*.md" --json
```

標準ライブラリのみ。出力は 3 部構成。

1. **文体の骨格** — 1 文の平均字数 / 25 字以下の割合 / 平板な文末の割合 / 文頭接続詞の割合
2. **癖の検出** — ダッシュ・語尾のぼかし・読者代弁の枕・中身のない繋ぎ・接続詞の偏り・逐語重複・内輪語ほか
3. **自動では出ない項目** — 見出し・一貫性・温度・具体性（人間が読む）

**数値は合否ではなく目安。** WARN が出た箇所を読んで判断する。定義済みのキーワードを
意図的に繰り返している場合など、正当な理由があれば無視してよい。

**適用範囲**: 目安値は「敬体（です・ます）で書かれた読者向けの本文」を前提にしている。
技術文書・仕様書・箇条書き主体の md に当てても意味のある数値は出ない（癖の検出部だけは使える）。

---

## ワークフロー

### A. 既にある下書きを推敲・改稿する

1. **`prose_check.py` を実行**して現状を数値化する
2. **層を切り分ける**（構成／温度／手触り）。ユーザーの言葉だけで決めず、数値と合わせて判断する
3. 該当する参照ファイルを読む
   - 構成の問題 → `references/structure.md`
   - 温度の問題 → `references/prose-tics.md` の F1
   - 手触りの問題 → `references/prose-tics.md` の F2
   - 個別の癖 → `references/prose-tics.md` の A〜E
4. **修正する。事実・数字は変えない**
5. **`prose_check.py` を再実行**する（Critical 参照）
6. **数値を変えていないことを機械的に照合する**（下の「事実の保全」）
7. before / after の指標を添えて報告する

### B. 新しく書く

1. **幹の一文**と**章ごとの一文**を先に固定する → `references/structure.md`
2. 書く前に `references/prose-tics.md` の A 章（AI 文の指紋）だけ読む。ここを避けるだけで初稿の質が変わる
3. 書く
4. `prose_check.py` → 修正 → 再実行

### C. 多観点レビューを設計する

`references/review-lenses.md` を読む。**表現校正の担当を置き忘れないこと**（実際に置き忘れて、
「文として成立していない箇所」を取り逃した事故がある）。**通し一貫性の担当は分割不可。**

---

## 事実の保全（数字を扱う文書では必須）

推敲で表現を変えるとき、数値まで書き換えていないかを機械的に照合する。目視では漏れる。

```bash
# 主要な数値の出現回数を、変更前（git HEAD）と現在で突き合わせる
for n in "20,891" "34.2" "0.313" "86%"; do
  a=$(git show HEAD:draft.md | grep -o -F "$n" | wc -l)
  b=$(grep -o -F "$n" draft.md | wc -l)
  [ "$a" = "$b" ] && echo "$n OK" || echo "$n ←差分あり 前=$a 後=$b"
done
```

差分が出たら、意図した追記か、事故かを必ず確認する。

## 感情を書くときの制約

書き手の内面は**他人には確認しようがない**。読者向けに感情を書き足す場合:

- **内面（感じたこと）は提案してよい**が、必ず本人に確認を出す
- **具体的な出来事（日付・回数・行動）は創作しない**。「7 敗目にコードを読み直した」のような
  検証可能な事実は、作った時点で虚偽になる
- 有料コンテンツ・対外公開物では、追記した感情記述を**一覧にして本人に提示する**

## 情報の鮮度

数字や結論を本文に書く前に、**その主題について後から出た記録がないかを必ず確認する。**
前の結論が後発の調査で覆っていた実例がある。1 件の資料だけを根拠に外部公開物へ書かない。

---

## 参照ファイル

| ファイル | 中身 | いつ読むか |
|---|---|---|
| `references/prose-tics.md` | 癖カタログ 18 項目と直し方、淡白さの正体（F 章） | 推敲時。書く前は A 章だけ |
| `references/structure.md` | 設計原則 A–E、具体を残す基準、社内文書転写の失敗型 | 構成を決めるとき |
| `references/review-lenses.md` | 6 観点の担当設計、最浅層読者 rubric、自動反復ハーネス | レビュー体制を組むとき |
